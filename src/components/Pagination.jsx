// src/components/Pagination.js
import React from 'react';

const Pagination = ({ onPageChange }) => {
  return (
    <div className="flex justify-center space-x-2 mt-4">
      {/* Agrega botones de paginación aquí */}
    </div>
  );
};

export default Pagination;
